services:
  python-app:
    image: python:3.11-slim
    volumes:
      - .:/app
    ports:
      - "5050:5050"
    working_dir: /app
    tty: true
    stdin_open: true
    command: sh -c "apt-get update && apt-get install -y git && python3"